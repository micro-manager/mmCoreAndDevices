

includes = [
    include_directories('.') # include the main directory
    ]

deps = [
	boost_dep,
	MMDevice_dep
	]
	
sources = [  # Must explicitly list all source files.
	'PI.cpp'
]


shared_library('PI', sources, dependencies : deps, install: true)

