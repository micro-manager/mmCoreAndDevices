

includes = [
    include_directories('.') # include the main directory
    ]

deps = [
	boost_dep,
	MMDevice_dep
	]
	
sources = [  # Must explicitly list all source files.
	'Vincent.cpp'
]


shared_library('Vincent', sources, dependencies : deps, install: true)

