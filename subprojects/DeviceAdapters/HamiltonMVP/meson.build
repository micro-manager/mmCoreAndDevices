

includes = [
    include_directories('.') # include the main directory
    ]

deps = [
	MMDevice_dep
	]
	
sources = [  # Must explicitly list all source files.
	'HamiltonMVP.cpp'
]


library('HamiltonMVP', sources, include_directories: includes, dependencies : deps, install: true)

