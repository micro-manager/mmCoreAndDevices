

includes = [
    include_directories('.') # include the main directory
    ]

deps = [
	boost_dep,
	MMDevice_dep
	]
	
sources = [  # Must explicitly list all source files.
	'Prior.cpp'
]


shared_library('Prior', sources, dependencies : deps, install: true)

